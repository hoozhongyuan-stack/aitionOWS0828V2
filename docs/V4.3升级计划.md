# AitionOWS V4.3 升级计划与验收记录(第三主题包「禾野 · Harvest」)

> 分支 feat/v41-v42 · 基于 V4.1/V4.2 · 仅本地验收(暂不部署生产)
> 方向已确认:A · 沃野丰收(麦绿+麦穗金+米白,农产品电商/食品转化友好)

## 设计定义

- **命名**:禾野 · Harvest(preset key = harvest)
- **色彩**:主色作物绿 #4A7C43 / 强调丰收金 #D9A441 / 背景暖米白 #FAF7F0 / 文字墨绿 #2C3327 / 次要文字大地棕 #8B6F4E(对比度按 WCAG AA)
- **质感**:暖纸底+极淡编织纹理(CSS 实现,零图片资产)、卡片大圆角暖边框、阳光光斑固定层
- **图形**:区块标题前的麦穗 SVG 装饰(低透明度 data-uri,克制使用)
- **字体**:标题衬线栈('Noto Serif SC' 回退 Songti/Georgia),正文保持无衬线
- **动效**:自然生长系——滚动显现 fade+上移(复用 aurora-reveal 类,harvest 段覆盖为柔和曲线)+装饰微 sway;无霓虹无强视差;prefers-reduced-motion 自动降级

## 实现清单(零迁移,零业务逻辑改动)

1. settings API themeSchema:preset 枚举 + harvest
2. 根 layout:data-theme 挂载判定扩展(aurora|harvest)
3. globals.css:[data-theme="harvest"] 作用域段(基底/纹理/卡片/按钮/标题/hero/装饰/动效;与 aurora 段同模式,classic 零改动)
4. aurora-motion.tsx:启用条件扩展(aurora|harvest;视差 harvest 用柔和系数)
5. 后台主题页:选择卡加「禾野」(含推荐调色板一键填充);header 角色徽章不变

## 验收要点

- 三主题热切换互不影响;classic/aurora 零回归
- harvest 全页面走查:首页/栏目/商品/购物车/结算/个人中心/后台
- 后台调色板在 harvest 下仍可覆盖(主题=结构+默认色,调色板=用户色)

## 内置浏览器深度测试记录(2026-09-07)

| # | 用例 | 结果 |
|---|---|---|
| 1 | 基线:aurora 首页截图(深空底/霓虹/毛玻璃/价格金色) | ✅ 零回归基线 |
| 2 | API 切 harvest → data-theme=harvest 挂载 | ✅ |
| 3 | harvest 首页(1440px):暖米白纸纹/衬线标题+麦穗装饰/麦绿 CTA/暖白圆角卡片 | ✅ |
| 4 | harvest 商品栏目页(magazine):筛选条暖白/页签麦绿/子分类页签 | ✅ |
| 5 | harvest 商品详情:价格 primary 绿/加入购物车麦绿/规格表/纸纹 | ✅ |
| 6 | 购物车页(DOM 快照):标题/商品行/金额/去结算/继续购物 | ✅ 5/5 |
| 7 | 结算页(快照):邮箱框/提交订单/订单摘要 | ✅ 3/3 |
| 8 | 后台主题页:经典/极光/禾野三卡并存 | ✅ |
| 9 | H5 视口 375px:header 折叠/纸纹/麦穗/CTA 响应式 | ✅ 无破版 |
| 10 | classic 回归:纯白底无纸纹无麦穗无衬线(截图) | ✅ 零残留 |
| 11 | aurora 回归:data-theme=aurora 挂载 | ✅ |
| 12 | 测试后恢复 harvest(供用户验收) | ✅ |

观察项(非缺陷,记录):① Hero 图为内容轮播图(蓝色办公室图),禾野下图片本身不变属内容层;② 富文本正文内的 h2 也会带麦穗装饰(风格统一设计,空标题时会呈孤立穗形——标题有文字时正常);③ 演示商品封面文件缺失处显示 alt 文本(本地演示数据无图,生产有真实图不影响)。

## 代码质量与安全审查记录(2026-09-08)

**门禁三件套**:tsc 0 错 / ESLint 0 warning(清理 8 处未使用变量:account tShop、首页 Props、商品详情 tShop、admin-users req、CtaBanner setHidden、order ShopConfig、admin-users 三个 Card 组件)/ build 通过 / vitest 199 全绿。

**安全审查(分支 feat/v41-v42 全量,V4.1+V4.2+V4.3 一起发布)**:
- CSS 注入面:harvest 段为静态 CSS,无用户输入拼接;调色板仍走 themeSchema COLOR 正则校验(V3.2 既有防御);preset 用户可控值收敛于三值枚举 ✅
- XSS:售后原因等新 UGC 字段 React 渲染自动转义;邮件模板走 escapeMultiline ✅;无新增 dangerouslySetInnerHTML ✅
- 越权:售后归属双因子校验(userId/email);admin-users/admin-logs requireOwner;订单操作 requirePerm(commerce)——22 路由守卫分布已 grep 复核(auth/login、logout 公开,auth/password 登录即可,均合理) ✅
- 金额防篡改:售后金额服务端校验≤实付;购物车价格结算时服务端重算 ✅
- SQL 注入:全 Prisma 参数化,新增 groupBy/findMany where 均对象构造 ✅
- 文件上传:folderId 仅整数关联,上传类型/大小校验沿用既有机制 ✅
- 已知边界(记录不修):① staff 敲 URL 可见无权限页面壳(API 数据已 403,侧栏无入口)——企业内网场景可接受;② media 路由守卫失败时也记 media.post 日志(adminName="?",失败尝试审计语义);③ 后台面板随主题变色(与 aurora 现状一致)
