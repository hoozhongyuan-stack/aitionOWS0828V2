# AitionOWS V4.0 升级计划(海外独立站起步 · 已确认)

> 分支:`feat/v4-commerce` · 版本:4.0.0 · 基线:main(3.3.0)
> 已确认:① 分支名/版本号;② 购物车客户端方案;③ 订单 5 态状态机;④ 不做清单。
> 流程:计划确认 → 开发 → 用户验收 → 验收通过后再部署生产。

---

## 主线 E1 · 商品交易化

### 数据模型(一次迁移,纯增量)

```
Content 增加两列:
  priceCents Int?     // 价格(最小货币单位,如"分");null=不展示价格,仅询盘
  currency   String?  // ISO 4217(USD/EUR…);null=用站点默认币种
Setting 新组 shop:
  currency     String  // 站点默认币种(缺省 USD)
  paymentInfo  String  // 线下付款指引(银行账户/PayPal 等,多行文本,后台可编辑)
```

价格存整数「分」避免浮点误差;展示用 `Intl.NumberFormat`(币种+地区格式化)。
**价格留空 = 仅询盘**(商品页不显示购买按钮)——天然支持「可购买 + 可询盘」混合模式。

### 页面变化

**后台**:
| 页面 | 变化 |
|---|---|
| 内容编辑页(商品类型) | 「基本信息」区新增:价格(数字,留空=仅询盘)、币种(下拉,缺省站点默认) |
| 站点配置 → **商店设置(新页)** | 默认币种下拉(常见币种)、线下付款指引多行文本;保存即生效 |

**前台**:
| 页面 | 变化 |
|---|---|
| 商品卡片(栏目页/首页楼层) | 有价格时显示价格(币种格式化);无价格 = 现状不变 |
| 商品详情页 | 价格展示 + 「Add to Cart」按钮(有价格时);无价格仅「询盘表单」;点击加购后头部购物车角标 +1 |
| 商品栏目页(顶部) | **筛选条(新)**:关键词搜索(标题/摘要)+ 价格区间(最低/最高)+ 排序(最新/价格从低到高/从高到低);URL searchParams 驱动,服务端过滤保 SEO |

### JSON-LD offers(Google 免费商品列表)

- 商品详情 JSON-LD 的 Product 节点增加 `offers`(price/priceCurrency/availability=InStock)
- 无价格商品不输出 offers(保持现状合法);回归测试锁定

---

## 主线 E2 · 交易 MVP(线下付款 + 后台审核)

### 购物车(客户端方案,零新表)

- localStorage 存 `{contentId, qty}` 列表;**结算时服务端按当前价格重算生成快照**(防前端篡改价格)
- 头部导航新增购物车图标(数量角标,实时);极光/经典双主题适配

### 数据模型(与 E1 同一次迁移,纯增量)

```
Order(
  id, no(唯一,如 AO-20260907-XXXX), status,
  userId?(登录用户关联;游客下单为 null), email, name, phone,
  country, address, city, zip, note(买家备注),
  currency, itemsTotalCents, shippingCents, grandTotalCents,
  adminNote?, createdAt, confirmedAt?, shippedAt?, cancelledAt?
)
OrderItem(id, orderId, contentId, titleSnapshot, priceCentsSnapshot, currency, qty)
```

### 订单状态机(5 态,用户已确认增加「已完成」)

```
pending 待确认 → confirmed 已确认收款 → shipped 已发货 → completed 已完成
                    ↓(任意非终态可取消)
                 cancelled 已取消
```

| 状态 | 触发者 | 邮件(买家) |
|---|---|---|
| pending | 买家提交订单 | 下单确认(含订单号 + 线下付款指引 + 金额) |
| confirmed | 后台「确认收款」 | 收款确认(订单进入备货) |
| shipped | 后台「标记发货」(可填物流备注) | 发货通知(含物流备注) |
| completed | 后台「标记完成」 | 无(终态) |
| cancelled | 后台取消(或买家限时未付款后台清理,本版仅手动) | 取消通知 |

管理员在「pending/confirmed」等关键节点同步收到站内通知邮件(复用 `notifyAdmin`)。
邮件模板沿用代码层品牌模板(后台可配 SMTP 通道与商店设置里的付款指引内容;模板文案本身在代码,后台不可编辑——列入不做清单)。

### 页面变化

**前台(3 个新页 + 头部)**:
| 页面 | 内容 |
|---|---|
| `/cart` 购物车页 | 条目列表(图/标题/单价/数量步进/小计/删除)、合计、空车状态、「去结算」 |
| `/checkout` 结算页 | 收货信息表单(email/姓名/电话/国家/地址/城市/邮编/备注;游客可下单,登录用户自动带出资料)+ 订单摘要 + 「提交订单」 |
| `/order/success` 下单结果页 | 订单号、金额、**线下付款指引**(后台商店设置内容)、「我们确认收款后将邮件通知」 |
| 头部导航 | 购物车图标+角标(所有前台页面) |

**订单查询与防篡改**:
- 游客查询:确认邮件含查询链接(email + 订单号参数);结果页只读展示状态
- 下单限频:同邮箱/IP 每小时 5 单
- 服务端校验:商品必须 PUBLISHED、价格按服务端现价重算、数量 1~99

**后台(1 个新组 + 2 个新页)**:
| 页面 | 内容 |
|---|---|
| 侧栏新组「**交易**」→ **订单管理** | 列表:状态页签(全部/待确认/已确认/已发货/已完成/已取消)、订单号/邮箱搜索、分页;行内显示订单号/买家/金额/状态/时间 |
| 订单详情页 | 商品明细(快照标题/单价/数量/小计)、收货信息、买家备注、金额汇总;操作按钮按状态出现:「确认收款」/「标记发货」(填物流备注)/「标记完成」/「取消订单」(填原因);管理员备注;每次操作二次确认 |

---

## 主线 E3 · GDPR(Cookie 同意 + 隐私政策)

### 页面变化

| 页面 | 变化 |
|---|---|
| 全站(首次访问) | 底部 Cookie 同意条幅:文案 + 「全部接受 / 仅必要」两按钮;选择存 localStorage,之后不再弹;不阻塞渲染;双语言 |
| `/agreement/privacy` 隐私政策页 | 沿用现有协议页体系(后台「语言管理 → 用户协议」可编辑),内容模板补充 GDPR 要素(数据收集范围/用途/用户权利/联系方式) |
| `/agreement/cookies` **Cookie 政策页(新类型)** | 协议体系新增 cookies 类型,后台可编辑;说明必要 Cookie 与同意记录用途 |
| footer | 新增 Privacy Policy / Cookie Policy / Terms 链接(多语言) |

---

## 数据库迁移

一次迁移(`20260907xxxxx_v40_commerce`),纯增量:
- `Content` 加 2 列(priceCents/currency,可空)
- 新建 `Order`/`OrderItem` 两表
- 存量数据零影响;生产库无 shop 设置 = 站点默认币种 USD/付款指引为空(下单结果页指引显示后台未配置提示)

## 不做清单(本版边界,已确认)

- 在线支付网关(Stripe/PayPal 集成——主体就绪后 V4.x)
- SKU 变体 / 库存管理
- 多币种换算(单币种定价;币种仅决定展示格式)
- 税务计算 / 发票
- 物流商 API(运费为站点级简单设置:固定运费+免邮门槛,**可后台配置**)
- 后台邮件模板编辑器

## 排期

| 阶段 | 内容 | 量级 |
|---|---|---|
| 1 | E1 商品交易化(字段/编辑/展示/JSON-LD/筛选) | ~4 天 |
| 2 | E2 交易 MVP(购物车/checkout/订单服务/邮件/后台订单) | ~1.5 周 |
| 3 | E3 GDPR(条幅/协议页扩展/footer) | ~1 天 |
| 4 | 门禁(测试全绿/构建)+ 本地演示 | ~0.5 天 |

合计约 **2.5~3 周**。**验收通过后另行执行部署**(打包桌面/推送/备份生产/部署)。

## 验收要点(对应页面变化清单)

1. 后台商品填价格 → 前台卡片/详情显示价格,JSON-LD 出现 offers;价格留空 → 仅询盘,与现状一致
2. 栏目页筛选条:关键词/价格区间/排序生效,URL 可分享
3. 加购 → 头部角标 → 购物车改数量/删除 → 结算提交 → 收到下单邮件(含付款指引) → 后台订单出现(pending)
4. 后台确认收款/发货/完成 → 各触发买家邮件;取消同理
5. 首次访问出 Cookie 条幅,选择后不再弹;privacy/cookies 页可访问且后台可编辑;footer 链接齐全
6. 存量内容/页面/后台各功能零回归(179+ 测试全绿)
