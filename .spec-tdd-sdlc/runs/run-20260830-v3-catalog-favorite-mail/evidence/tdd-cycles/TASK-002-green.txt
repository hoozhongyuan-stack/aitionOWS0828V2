== TASK-002 GREEN (TEST-017 / AC-015 / NFR-003) ==

[决策记录(用户已批准)] prisma migrate diff 对带外键的 Content 表一律生成重建式 SQL(CREATE new_Content + INSERT..SELECT + DROP TABLE + RENAME;已实测 4 种组合:from-migrations/from-schema-datamodel × 新列居中/置尾,均含 DROP/RENAME;触发条件为表上的外键约束,User 无外键则输出纯 ADD COLUMN),与已批准的『仅加列/建表,不改不动既有列』决策冲突。经用户决策:按 20260828010000_form_submission_status 先例手写纯增量 SQL,并以 migrate diff 收敛校验兜底。

[收敛校验] command: npx prisma migrate diff --from-migrations prisma/migrations(含新迁移) --to-schema-datamodel prisma/schema.prisma --shadow-database-url file:<tmp>/converge.db --script
diff_exit=0
output: -- This is an empty migration.  (手写迁移与 schema 完全收敛)

[测试修正记录] 首次 GREEN 后发现 beforeAll 内 legacy. 未 await(与 migrate deploy 竞态,表现为偶发 user 行缺失),修正为 async beforeAll + await;修正后连续 3 次运行全部通过。该修正属测试夹具缺陷,非被测行为变更。

== TEST-017 迁移演练(最终) ==
command: npx vitest run tests/db/migration.test.ts
(!) Your Vite config uses features that are unsupported by `configLoader: 'native'`, which is planned to become the default in a future major version of Vite:
  - ESM syntax in a file loaded as CommonJS (vitest.config.ts:1:1). Use a `.mjs` extension or set `"type": "module"` in the closest package.json
Set `VITE_CONFIG_NATIVE_IGNORE_WARNING=true` to suppress this warning.

 RUN  v4.1.11 /Users/hoo/Documents/Zcode_GLM/AitionOWS_V1.2.3

 ✓ tests/db/migration.test.ts (1 test) 1706ms
     ✓ 应用 V3.0 迁移后:存量数据无损、新列空值/默认值、Favorite 唯一约束、迁移幂等  1078ms

 Test Files  1 passed (1)
      Tests  1 passed (1)
   Start at  22:33:28
   Duration  2.67s (transform 23ms, setup 678ms, import 32ms, tests 1.71s, environment 0ms)


exit_code: 0
timestamp_utc: 2026-08-30T14:33:30+0000

== 迁移 SQL 全文(prisma/migrations/20260830120000_v3_product_favorite_user_profile/migration.sql,手写纯增量,零 DROP/RENAME) ==
-- ============================================================
-- V3.0 迁移:商品图集/规格参数 + 收藏域 + 用户公司地区资料
-- 对应需求:REQ-001(gallery/specs)、REQ-005(Favorite/favoriteCount)、REQ-009(User 资料)
-- 策略:仅加列/建表,不改不动既有列;新列可空或带默认值,对存量数据无损;
--       可重复执行(prisma migrate deploy 幂等登记,不重复应用)。
-- 说明:Content 表带外键约束,prisma migrate diff 会生成重建式 SQL
--       (CREATE new_Content + INSERT..SELECT + DROP TABLE + RENAME),
--       与"仅加列/建表"决策冲突,故本迁移按 20260828010000_form_submission_status
--       先例手写纯增量 SQL;已用 migrate diff(--from-migrations 含本迁移)
--       校验与 schema.prisma 收敛为空差异(见 TASK-002-green.txt 证据)。
-- ============================================================

-- AlterTable:内容表增加商品图集与规格参数(JSON 字符串),及收藏计数
ALTER TABLE "Content" ADD COLUMN "gallery" TEXT;
ALTER TABLE "Content" ADD COLUMN "specs" TEXT;
ALTER TABLE "Content" ADD COLUMN "favoriteCount" INTEGER NOT NULL DEFAULT 0;

-- AlterTable:用户表增加公司/地区资料(后台用户管理维护,全部可空)
ALTER TABLE "User" ADD COLUMN "companyName" TEXT;
ALTER TABLE "User" ADD COLUMN "country" TEXT;
ALTER TABLE "User" ADD COLUMN "province" TEXT;
ALTER TABLE "User" ADD COLUMN "city" TEXT;

-- CreateTable:收藏记录(targetId 通用引用,无外键,应用层维护一致性;
-- 唯一约束兜底防重复,增删时由应用层同步 Content.favoriteCount)
CREATE TABLE "Favorite" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "targetType" TEXT NOT NULL DEFAULT 'CONTENT',
    "targetId" INTEGER NOT NULL,
    "userId" INTEGER NOT NULL,
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
);

-- CreateIndex
CREATE UNIQUE INDEX "Favorite_targetType_targetId_userId_key" ON "Favorite"("targetType", "targetId", "userId");

-- CreateIndex
CREATE INDEX "Favorite_userId_idx" ON "Favorite"("userId");

== 本地库迁移应用 ==
前置:备份 backups/pre-v3-migration/app-20260830222215.db(WAL checkpoint TRUNCATE 返回 0|0|0)已确认存在
command: npx prisma migrate deploy
output: 3 migrations found; Applying migration 20260830120000_v3_product_favorite_user_profile; All migrations have been successfully applied.
deploy_exit=0
command: sqlite3 data/app.db "PRAGMA integrity_check;" -> ok
抽查:Content 4 行/密码外数据完好且 favoriteCount=0;User 1 行,新增 companyName/country/province/city 可空列;Favorite 表含唯一索引 Favorite_targetType_targetId_userId_key 与索引 Favorite_userId_idx,0 行

== 全量回归 ==
command: npx vitest run
(!) Your Vite config uses features that are unsupported by `configLoader: 'native'`, which is planned to become the default in a future major version of Vite:
  - ESM syntax in a file loaded as CommonJS (vitest.config.ts:1:1). Use a `.mjs` extension or set `"type": "module"` in the closest package.json
Set `VITE_CONFIG_NATIVE_IGNORE_WARNING=true` to suppress this warning.

 RUN  v4.1.11 /Users/hoo/Documents/Zcode_GLM/AitionOWS_V1.2.3

 ✓ tests/smoke.test.ts (3 tests) 81ms
 ✓ tests/db/migration.test.ts (1 test) 1581ms
     ✓ 应用 V3.0 迁移后:存量数据无损、新列空值/默认值、Favorite 唯一约束、迁移幂等  1039ms

 Test Files  2 passed (2)
      Tests  4 passed (4)
   Start at  22:34:21
   Duration  2.73s (transform 59ms, setup 1.68s, import 39ms, tests 1.66s, environment 0ms)

vitest_exit=0

command: npx tsc --noEmit
(无输出即零类型错误) tsc_exit=0

regression_timestamp_utc: 2026-08-30T14:34:39+0000
